PySide6>=6.6
pydantic>=2

# Optional but recommended for richer Excel/data support:
pandas>=2.0
openpyxl>=3.1
defusedxml>=0.7.1
keyring>=25.0
rapidfuzz>=3.0
python-dotenv>=1.0

# Optional exact optimizer. The app falls back to the local heuristic if unavailable.
ortools>=9.10
